1、django.template.exceptions.TemplateDoesNotExist: login.html

2、默认放模板htmk的目录  要手动加一下  os.path.join(BASE_DIR, 'templates')
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]


3、network选项里面的ssh.exe为git的  在usr/bin下







4、                     mysqld --install   

    用cmd在管理员窗口 bin下  先添加mysql服务  mysqld --install
	然后在启动mysql   mysqld --install